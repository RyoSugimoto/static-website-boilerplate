extends /layouts/default.pug

block props
  -
    title = `FAQ - ${Config.name}`
    root = '..'
    url += '/faq/'
    breadcrumbList = [
      {
        label: 'FAQ'
      },
    ]

block head

block main
  +page-title() FAQ
  +container('lg')
    +text-content()

